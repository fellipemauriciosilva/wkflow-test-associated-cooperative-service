package com.cooperative.domain.entity;

/**
 * Represents the outcome of a voting session on an Agenda.
 * Placeholder for Phase 4 result calculation logic.
 * Plain Java enum, no framework dependencies.
 */
public enum VotingResult {
    APPROVED,
    REJECTED,
    TIE
}
